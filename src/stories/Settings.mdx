import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Настройка"/>

# Настройки библиотеки

## Установка библиотеки

### Основной пакет

```bash
npm install isp-admin-ui-kit
```

### Дополнительные зависимости

Библиотека требует установки следующих зависимостей для корректной работы:

```bash
npm install @monaco-editor/react antd axios dayjs monaco-editor react react-dom react-hook-form react-router-dom react-redux @reduxjs/toolkit swagger-ui-react
```

**Версии зависимостей**

| Пакет | Версия |
|-------|--------|
| `@monaco-editor/react` | `^4.7.0` |
| `@reduxjs/toolkit` | `^2.2.3` |
| `antd` | `>=5.12.2` |
| `axios` | `^1.7.7` |
| `dayjs` | `^1.11.10` |
| `monaco-editor` | `^0.55.1` |
| `react` | `>=18.2.0 <19` |
| `react-dom` | `>=18.2.0 <19` |
| `react-hook-form` | `^7.54.0` |
| `react-redux` | `^9.1.0` |
| `react-router-dom` | `^6.22.3` |
| `swagger-ui-react` | `^5.31.0` |

---

## Настройка Monaco Editor

> ⚠️ **Критически важно:** Компоненты с редактором кода не будут работать без правильной настройки loader.

В точке входа проекта (`src/index.ts` или `src/main.tsx`) необходимо выполнить инициализацию **до** рендера приложения:

```tsx
import * as monaco from 'monaco-editor';
import loader from '@monaco-editor/react';

// Конфигурация
loader.config({ monaco });

// Инициализация
await loader.init();
```

**Полный пример точки входа:**

```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import * as monaco from 'monaco-editor';
import loader from '@monaco-editor/react';
import { baseSetupStore, AdminBase } from 'isp-admin-ui-kit';
import { Provider } from 'react-redux';
import { BrowserRouter } from 'react-router-dom';

const init = async () => {
  // 1. Monaco Editor
  loader.config({ monaco });
  await loader.init();

  // 2. Store
  const store = baseSetupStore();

  // 3. Render
  ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
      <Provider store={store}>
        <BrowserRouter>
          <AdminBase />
        </BrowserRouter>
      </Provider>
    </React.StrictMode>
  );
};

init();
```

## Настройка Monaco Worker (Vite / Rollup)

⚠️ Важно: Если ваше приложение или библиотека собирается через Vite или Rollup, необходимо явно подключить Monaco Workers.
Без этого редактор может выдавать ошибки или не работать подсветка синтаксиса.

🔧 Пример настройки

Добавьте конфигурацию до инициализации loader:
```jsx
import { loader } from '@monaco-editor/react';
import * as monaco from 'monaco-editor';

// импорт worker-файлов
import editorWorker from 'monaco-editor/esm/vs/editor/editor.worker?worker';
import cssWorker from 'monaco-editor/esm/vs/language/css/css.worker?worker';
import htmlWorker from 'monaco-editor/esm/vs/language/html/html.worker?worker';
import jsonWorker from 'monaco-editor/esm/vs/language/json/json.worker?worker';
import tsWorker from 'monaco-editor/esm/vs/language/typescript/ts.worker?worker';

// настройка окружения Monaco
self.MonacoEnvironment = {
    getWorker(_: unknown, label: string) {
        if (label === 'json') return new jsonWorker();
        if (label === 'css' || label === 'scss' || label === 'less') return new cssWorker();
        if (label === 'html' || label === 'handlebars' || label === 'razor') return new htmlWorker();
        if (label === 'typescript' || label === 'javascript') return new tsWorker();
        return new editorWorker();
    },
};

// конфигурация loader
loader.config({ monaco });

// инициализация
loader.init();
```

## Работа с компонентом AdminBase

Компонент `AdminBase` — это основной компонент библиотеки, который предоставляет готовую структуру админ-панели.

### Параметры компонента

| Название | Описание | Пример |
|----------|----------|--------|
| `customRouters` | Массив объектов маршрутов | `[{ route, element, label, key, ... }]` |
| `configProviderProps` | Пропсы для `ConfigProvider` из Ant Design | `{ theme: lightTheme, locale: ru }` |
| `defaultRoutePath` | Путь для редиректа с главной страницы (`/`) | `"/modules"` |
| `excludePermissions` | Массив пермишенов для скрытия страниц | `[PermissionKeysType.user_view]` |

### Примеры использования

**Базовое использование:**

```tsx
import AdminBase from 'isp-admin-ui-kit';

<AdminBase />;
```

**Полная конфигурация:**

```tsx
<AdminBase
  customRouters={customRouters}
  configProviderProps={{ theme: lightTheme, locale: ru }}
  defaultRoutePath="/dashboard"
  excludePermissions={excludePermissions}
/>;
```

---

## Подключение базового стора

### Инициализация

```tsx
import { baseSetupStore } from 'isp-admin-ui-kit';
import { Provider } from 'react-redux';
import { BrowserRouter } from 'react-router-dom';

const store = baseSetupStore();

<Provider store={store}>
  <BrowserRouter>
    <AdminBase />
  </BrowserRouter>
</Provider>;
```

### Работа с базовыми API

Для получения данных из встроенных сервисов используйте `baseApiServices`:

```tsx
import { baseApiServices } from 'isp-admin-ui-kit';

const { data, isLoading, isError } = baseApiServices.roleApi.useGetAllRolesQuery();
```

---

## Интеграция с кастомным стором

Функция `baseSetupStore` принимает необязательный параметр `apiServices` для подключения пользовательских редьюсеров и RTK Query сервисов.

### Параметры

| Название | Описание | Пример |
|----------|----------|--------|
| `apiServices` | Объект кастомных редьюсеров и слайсеров | `{ modulesServiceApi, UIReducer }` |

### Пример

```tsx
import { baseSetupStore } from 'isp-admin-ui-kit';

const apiServices = {
  modulesServiceApi,
  UIReducer,
};

const store = baseSetupStore(apiServices);
```

---

## Подключение темы

Библиотека использует тему Ant Design. Для кастомизации передайте параметры через `configProviderProps`:

```tsx
import AdminBase from 'isp-admin-ui-kit';
import { lightTheme } from './theme';
import { ru } from 'antd/locale';

<AdminBase configProviderProps={{ theme: lightTheme, locale: ru }} />;
```

Для добавления динамических стилей к кастомным компонентам используйте CSS-переменные Ant Design.

---

## Настройка роутеров

Компонент `AdminBase` поддерживает кастомную маршрутизацию через параметр `customRouters`.

> ⚠️ **Важно:** Для корректной работы пунктов меню значения полей `route` и `key` должны совпадать!

### Параметры маршрута

| Название | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `route` | `string` | ✅ | Путь маршрута |
| `element` | `ReactNode` | ✅ | Компонент для отображения |
| `label` | `string` | ❌ | Название в меню |
| `key` | `string` | ✅ | Уникальный ключ (должен совпадать с `route`) |
| `permissions` | `string[]` | ❌ | Список разрешений для доступа |
| `icon` | `ReactNode` | ❌ | Иконка маршрута |
| `children` | `Array` | ❌ | Вложенные маршруты |
| `className` | `string` | ❌ | CSS-класс для пункта меню |

### Пример

```tsx
const customRouters = [
  {
    route: '/dashboard',
    key: 'dashboard',
    element: <Dashboard />,
    label: 'Dashboard',
    permissions: ['admin', 'user'],
    icon: <DashboardIcon />,
    className: 'custom-menu-item',
  },
];

<AdminBase customRouters={customRouters} />;
```

---

### Доступные пермишены

Ниже приведён полный список доступных пермишенов, которые можно использовать для управления доступом к страницам и функциям админ-панели.

### Профиль

| Ключ | Описание |
|------|----------|
| `profile_view` | Просмотр экрана «Профиль» |
| `profile_change_password` | Возможность смены пароля |

### Приложения и группы приложений

| Ключ | Описание |
|------|----------|
| `application_group_view` | Просмотр экрана «Группы приложений» |
| `application_group_add` | Добавление новой группы |
| `application_group_edit` | Редактирование существующей группы |
| `application_group_delete` | Удаление группы |
| `application_group_app_add` | Добавление нового приложения в группу |
| `application_group_app_edit` | Редактирование существующего приложения |
| `application_group_app_delete` | Удаление приложения из группы |
| `application_group_token_view` | Просмотр раздела «Токены» |
| `application_group_token_add` | Добавление нового токена |
| `application_group_token_delete` | Удаление токена |

### Доступы приложений

| Ключ | Описание |
|------|----------|
| `app_access_view` | Просмотр экрана «Доступы приложений» |
| `app_access_edit` | Редактирование элементов «Доступы приложений» |

### Переменные

| Ключ | Описание |
|------|----------|
| `variable_view` | Просмотр экрана «Переменные» |
| `variable_add` | Добавление новой переменной |
| `variable_edit` | Редактирование переменной |
| `variable_delete` | Удаление переменной |

### Модули

| Ключ | Описание |
|------|----------|
| `module_view` | Просмотр экрана «Модули» |
| `module_delete` | Удаление модуля |
| `module_configuration_save_unsafe` | Небезопасное сохранение конфигурации модуля |
| `module_history_set` | Установить выбранную версию модуля |
| `module_history_delete_version` | Удалить выбранную версию модуля |
| `module_configuration_edit` | Редактирование конфигурации модуля |
| `module_configuration_add` | Создание новой конфигурации |
| `module_configuration_set_active` | Сделать конфигурацию активной |

### Пользователи

| Ключ | Описание |
|------|----------|
| `user_view` | Просмотр экрана «Пользователи» |
| `user_block` | Блокировка пользователя |
| `user_create` | Создание пользователя |
| `user_update` | Обновление данных пользователя |
| `user_delete` | Удаление пользователя |

### Пользовательские сессии

| Ключ | Описание |
|------|----------|
| `session_view` | Просмотр экрана «Пользовательские сессии» |
| `session_revoke` | Отзыв сессии |

### Роли

| Ключ | Описание |
|------|----------|
| `role_view` | Просмотр экрана «Роли» |
| `role_add` | Создание роли |
| `role_update` | Обновление роли |
| `role_delete` | Удаление роли |

### Журналы информационной безопасности

| Ключ | Описание |
|------|----------|
| `security_log_view` | Просмотр экрана «Журналы ИБ» |

### Общий доступ

| Ключ | Описание |
|------|----------|
| `ALWAYS_VIEW` | Доступен всегда (для публичных страниц) |

---

## Пример использования

### Скрытие страниц через `excludePermissions`

```tsx
import { PermissionKeysType } from 'isp-admin-ui-kit';

const excludePermissions = [
  PermissionKeysType.user_view,
  PermissionKeysType.session_view,
  PermissionKeysType.role_view,
];

<AdminBase excludePermissions={excludePermissions} />;
```

### Проверка прав в компоненте

```tsx
import { useAppSelector } from 'isp-admin-ui-kit';
import { PermissionKeysType } from 'isp-admin-ui-kit';

const MyComponent = () => {
   const { hasPermission } = useRole()

  const isUpdateRole = hasPermission(PermissionKeysType.role_update)
  const isCreateRole = hasPermission(PermissionKeysType.role_add)

  return (
    <>
      {isUpdateRole && <UpdateButton />}
      {isCreateRole && <CreateButton />}
    </>
  );
};
```

### Настройка прав для маршрута

```tsx
import { PermissionKeysType } from 'isp-admin-ui-kit';

const customRouters = [
  {
    route: '/users',
    key: 'users',
    element: <UsersPage />,
    label: 'Пользователи',
    permissions: [PermissionKeysType.user_view],
    icon: <UsersIcon />,
  },
  {
    route: '/roles',
    key: 'roles',
    element: <RolesPage />,
    label: 'Роли',
    permissions: [PermissionKeysType.role_view],
    icon: <RolesIcon />,
  },
];

<AdminBase customRouters={customRouters} />;
```

---

> 💡 **Совет:** Для скрытия целых разделов рекомендуется использовать ключи `*_view` соответствующих пермишенов. Детальные права (`*_add`, `*_edit`, `*_delete`) используются для управления доступом к действиям внутри страниц.

## Обновление версии

Для обновления версии пакета:

1. Обновите значение `version` в `package.json`
2. Добавьте запись в `CHANGELOG.md`
3. Создайте коммит с сообщением: `up version to X.Y.Z`

```bash
git commit -m "up version to X.Y.Z"
git push
```
